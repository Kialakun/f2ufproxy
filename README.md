# **Forward Proxy to Upstrean Forward Proxy - Proxy**

A downstream proxy that adds credentials to requests before forwarding to an upstream forward proxy. If you ever find yourself in the situation where you need such a thing.
